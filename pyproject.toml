[build-system]
requires = ["scikit-build-core>=0.5", "pybind11>=2.11"]
build-backend = "scikit_build_core.build"

[project]
name = "shooter-rl"
version = "0.0.1"
requires-python = ">=3.8"

[tool.scikit-build]
cmake.build-type = "Release"
wheel.packages = ["shooter_rl"]
